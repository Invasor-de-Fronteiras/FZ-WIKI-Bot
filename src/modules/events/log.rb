module Bot::DiscordEvents
    module Mention
        extend Discordrb::EventContainer

message(contains: /(^|\s)fix($|\s)/i) do |event|
File.open('src/modules/events'"loggies.txt", "a") do |f|
f << "(#{event.timestamp})
MSG: #{event.message}
AUTHOR: #{event.author.username} (#{event.server.id})
SERVER NAME: #{event.server.name} (#{event.server.id})
SERVER ADM: #{event.server.owner.name} (#{event.server.owner.id})\n\n"
end
end

message(contains: /(^|\s)Hantzu($|\s)/i) do |event|
File.open('src/modules/events'"loggies.txt", "a") do |f|
f << "(#{event.timestamp})
MSG: #{event.message}
AUTHOR: #{event.author.username} (#{event.server.id})
SERVER NAME: #{event.server.name} (#{event.server.id})
SERVER ADM: #{event.server.owner.name} (#{event.server.owner.id})\n\n"
end
end

message(contains: /(^|\s)Malckyor($|\s)/i) do |event|
File.open('src/modules/events'"loggies.txt", "a") do |f|
f << "(#{event.timestamp})
MSG: #{event.message}
AUTHOR: #{event.author.username} (#{event.server.id})
SERVER NAME: #{event.server.name} (#{event.server.id})
SERVER ADM: #{event.server.owner.name} (#{event.server.owner.id})\n\n"
end
end

message(contains: /(^|\s)Fist($|\s)/i) do |event|
File.open('src/modules/events'"loggies.txt", "a") do |f|
f << "(#{event.timestamp})
MSG: #{event.message}
AUTHOR: #{event.author.username} (#{event.server.id})
SERVER NAME: #{event.server.name} (#{event.server.id})
SERVER ADM: #{event.server.owner.name} (#{event.server.owner.id})\n\n"
end
end

message(contains: /(^|\s)packets($|\s)/i) do |event|
File.open('src/modules/events'"loggies.txt", "a") do |f|
f << "(#{event.timestamp})
MSG: #{event.message}
AUTHOR: #{event.author.username} (#{event.server.id})
SERVER NAME: #{event.server.name} (#{event.server.id})
SERVER ADM: #{event.server.owner.name} (#{event.server.owner.id})\n\n"
end
end

message(contains: /(^|\s)custom quest($|\s)/i) do |event|
File.open('src/modules/events'"loggies.txt", "a") do |f|
f << "(#{event.timestamp})
MSG: #{event.message}
AUTHOR: #{event.author.username} (#{event.server.id})
SERVER NAME: #{event.server.name} (#{event.server.id})
SERVER ADM: #{event.server.owner.name} (#{event.server.owner.id})\n\n"
end
end

message(contains: /(^|\s)Ravi($|\s)/i) do |event|
File.open('src/modules/events'"loggies.txt", "a") do |f|
f << "(#{event.timestamp})
MSG: #{event.message}
AUTHOR: #{event.author.username} (#{event.server.id})
SERVER NAME: #{event.server.name} (#{event.server.id})
SERVER ADM: #{event.server.owner.name} (#{event.server.owner.id})\n\n"
end
end

message(contains: /(^|\s)launcher g10($|\s)/i) do |event|
File.open('src/modules/events'"loggies.txt", "a") do |f|
f << "(#{event.timestamp})
MSG: #{event.message}
AUTHOR: #{event.author.username} (#{event.server.id})
SERVER NAME: #{event.server.name} (#{event.server.id})
SERVER ADM: #{event.server.owner.name} (#{event.server.owner.id})\n\n"
end
end

message(contains: /(^|\s)Mai($|\s)/i) do |event|
File.open('src/modules/events'"loggies.txt", "a") do |f|
f << "(#{event.timestamp})
MSG: #{event.message}
AUTHOR: #{event.author.username} (#{event.server.id})
SERVER NAME: #{event.server.name} (#{event.server.id})
SERVER ADM: #{event.server.owner.name} (#{event.server.owner.id})\n\n"
end
end

message(contains: /(^|\s)this file($|\s)/i) do |event|
File.open('src/modules/events'"loggies.txt", "a") do |f|
f << "(#{event.timestamp})
MSG: #{event.message}
AUTHOR: #{event.author.username} (#{event.server.id})
SERVER NAME: #{event.server.name} (#{event.server.id})
SERVER ADM: #{event.server.owner.name} (#{event.server.owner.id})\n\n"
end
end

message(contains: /(^|\s)Luna($|\s)/i) do |event|
File.open('src/modules/events'"loggies.txt", "a") do |f|
f << "(#{event.timestamp})
MSG: #{event.message}
AUTHOR: #{event.author.username} (#{event.server.id})
SERVER NAME: #{event.server.name} (#{event.server.id})
SERVER ADM: #{event.server.owner.name} (#{event.server.owner.id})\n\n"
end
end

message(contains: /(^|\s)handlers($|\s)/i) do |event|
File.open('src/modules/events'"loggies.txt", "a") do |f|
f << "(#{event.timestamp})
MSG: #{event.message}
AUTHOR: #{event.author.username} (#{event.server.id})
SERVER NAME: #{event.server.name} (#{event.server.id})
SERVER ADM: #{event.server.owner.name} (#{event.server.owner.id})\n\n"
end
end

message(contains: /(^|\s)script($|\s)/i) do |event|
File.open('src/modules/events'"loggies.txt", "a") do |f|
f << "(#{event.timestamp})
MSG: #{event.message}
AUTHOR: #{event.author.username} (#{event.server.id})
SERVER NAME: #{event.server.name} (#{event.server.id})
SERVER ADM: #{event.server.owner.name} (#{event.server.owner.id})\n\n"
end
end
    
end
end